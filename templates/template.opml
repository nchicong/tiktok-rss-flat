<?xml version="1.0" encoding="UTF-8"?>
<opml version="1.0">
  <head>
    <title>Subscriptions of nchicong</title>
  </head>
  <body>
    {% for user in users -%}
      <outline text="{{ user.text }}" title="{{ user.title }}" type="rss" xmlUrl="{{ user.xmlUrl }}" />
    {% endfor %}
  </body>
</opml>
